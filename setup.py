from setuptools import setup, find_packages

setup(
  name='micronizer',
  version='',
  packages=find_packages(),
  url='',
  license='',
  author='koku',
  author_email='',
  description='',
  install_requires=[],
  extras_require={
    'test': ['pytest'],
  },
)
